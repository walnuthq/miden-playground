import Image from "next/image";
import Link from "@/components/tutorials/tutorial-link";

<Image
  className="max-h-96 object-contain"
  src="/img/count-copy-fpi-diagram.png"
  alt="Count copy FPI diagram"
  width={1100}
  height={744}
/>

The diagram above depicts the **Count Reader** smart contract using foreign procedure invocation to read the count state of the counter contract. After reading the state via FPI, the **Count Reader** smart contract writes the value returned from the counter contract to storage.

What we'll cover:

- **Foreign Procedure Invocation** (FPI) with the Playground.
- Building a **Count Reader** smart contract.
- Executing **cross-contract calls** in the browser.

First, let's discover the <Link href="/scripts/count-reader">Count Reader</Link> script logic.
