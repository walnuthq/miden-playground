import Link from "@/components/tutorials/tutorial-link";
import AccountAddress from "@/components/lib/account-address";
import CopyButton from "@/components/lib/copy-button";

- **Name**: Multisig Wallet <CopyButton content="Copy Account Name" copy="Multisig Wallet" />

- **Address**:

<AccountAddress account={props.account} withLink={false} />

Head to the <Link href="/accounts">accounts page</Link> and import the multisig wallet by clicking on the _"Create new account"_ button on top of the page and selecting the _"Import account"_ option.

A modal will show up requesting both a name and the address of your newly imported multisig.
